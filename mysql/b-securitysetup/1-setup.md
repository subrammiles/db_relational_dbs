# Secure MySQL Installation (Recommended)

Run the security script:

sudo mysql_secure_installation

It will ask things like:

Set root password

Remove anonymous users

Disallow remote root login

Remove test database

Answer Y (yes) for most prompts.

It works on most Linux distributions as long as MySQL Server is installed, because the script comes with MySQL itself,