Switch to PostgreSQL User

PostgreSQL creates a system user called postgres.

Switch to it:

sudo -i -u postgres



is used in Ubuntu/Linux to switch your terminal to the PostgreSQL system user.


Open the PostgreSQL shell:

psql

Prompt will look like:

postgres=#